export class Image {
    id: number;
    url: String;
    caption: String;

    constructor(id: number, url: String, caption: String) {
        this.id = id;
        this.url = url;
        this.caption = 
    }
}